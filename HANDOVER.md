# راهنمای تحویل — فروشگاه VYBE

این راهنما برای تو (کارفرما) نوشته شده، نه یک برنامه‌نویس. هیچ‌جای این سند نیازی به دانستن کدنویسی یا سرور نیست — فقط کار روزمره با پنل مدیریت.

---

## ۱. آدرس‌ها

| چی | آدرس |
|---|---|
| فروشگاه (چیزی که مشتری می‌بیند) | https://vybeshop.ir |
| پنل مدیریت (جایی که تو کار می‌کنی) | https://admin.vybeshop.ir |

پنل مدیریت را بوکمارک کن — هر روز با آن سروکار داری.

---

## ۲. ورود به پنل

با **شماره موبایل** و **رمز عبور** وارد می‌شوی (نه کد پیامکی — آن فقط برای مشتری‌های فروشگاه است).

اگر هنوز حساب مدیر خودت را نساخته‌ای، این کار یک‌بار توسط توسعه‌دهنده انجام می‌شود (به `SETUP-GUIDE.md` بخش ۰ نگاه کن). بعد از ساخته‌شدن، رمز را جایی امن (نه توی گوشی، نه توی چت) یادداشت کن.

**رمز را با هیچ‌کس در میان نگذار.** هر کسی که وارد پنل شود می‌تواند سفارش‌ها، محصولات، و تنظیمات پرداخت را تغییر بدهد.

---

## ۳. افزودن محصول جدید

۱. از منوی سمت راست، روی **«محصولات»** کلیک کن
۲. بالای صفحه، دکمه‌ی **«+ افزودن محصول»** را بزن
۳. فرم را پر کن: نام، قیمت، دسته‌بندی، توضیحات، عکس‌ها، و اگر محصول رنگ/سایز دارد، بخش «رنگ‌ها» را هم پر کن
۴. ذخیره کن

نکته‌ها:
- **عکس محصول را قبل از انتشار حتماً بارگذاری کن.** بدون عکس واقعی، محصول در فروشگاه بی‌کیفیت به نظر می‌رسد.
- موجودی محصول از بخش جدا («موجودی» در منو) مدیریت می‌شود — بعد از ساخت محصول، به آنجا برو و تعداد موجودی را وارد کن، وگرنه فروشگاه آن را «ناموجود» نشان می‌دهد.
- برای تغییر قیمت چند محصول با هم (مثلاً تخفیف فصلی)، به بخش **«اصلاح قیمت»** برو — نیازی نیست هر محصول را جدا باز کنی.

---

## ۴. پردازش یک سفارش

از منو روی **«سفارش‌ها»** کلیک کن. هر سفارش این مسیر را طی می‌کند:

```
در انتظار پرداخت → پرداخت‌شده → در حال پردازش → ارسال‌شده → تحویل‌شده
```

روی سفارش کلیک کن تا جزئیات باز شود. بسته به وضعیت فعلی سفارش، یک دکمه مناسب می‌بینی:

| وضعیت فعلی | دکمه‌ای که می‌زنی | چه اتفاقی می‌افتد |
|---|---|---|
| در انتظار پرداخت | «ثبت پرداخت» | فقط برای پرداخت‌های دستی/حضوری — پرداخت آنلاین خودش با تأیید درگاه به این وضعیت می‌رسد، نیازی به دست‌زدن نیست |
| پرداخت‌شده | «شروع پردازش» | یعنی داری بسته را آماده می‌کنی |
| در حال پردازش | کد رهگیری پستی را وارد کن، بعد «ثبت ارسال» | مشتری از این لحظه کد رهگیری را می‌بیند |
| ارسال‌شده | «تحویل داده شد» | سفارش بسته می‌شود |

اگر سفارشی باید لغو شود (تا وقتی به مرحله «ارسال‌شده» نرسیده)، دکمه‌ی قرمز **«لغو سفارش»** همیشه در دسترس است — یک دلیل کوتاه بنویس، اختیاری است.

**فاکتور و صورت‌حساب:** از همان صفحه‌ی جزئیات سفارش می‌توانی PDF فاکتور را دانلود کنی.

---

## ۵. اتصال پیامک (کاوه‌نگار) و درگاه پرداخت (زرین‌پال)

این دو، ستون فروشگاه‌اند:
- **بدون کاوه‌نگار، هیچ مشتری‌ای نمی‌تواند وارد سایت شود** (ورود فقط با کد پیامکی است)
- **بدون زرین‌پال، هیچ پرداختی انجام نمی‌شود**

مراحل دقیق ثبت‌نام و وارد کردن کلیدها در فایل **`SETUP-GUIDE.md`** (بخش‌های ۱ و ۲.۱) قدم‌به‌قدم توضیح داده شده — همان فایل را باز کن و دنبال کن.

⚠️ **یک نکته حیاتی که به‌سادگی فراموش می‌شود:** اعتبار کاوه‌نگار را هر چند وقت چک کن (از داشبورد پنل، بخش «سلامت سیستم»). اگر اعتبار تمام شود، ورود مشتری‌ها بی‌سروصدا از کار می‌افتد — نه پیامی، نه خطایی، فقط مشتری دیگر نمی‌تواند وارد شود.

---

## ۶. چه کارهایی نباید بکنی

- **رمز پنل را با کسی به اشتراک نگذار** و برای هر نفری که به پنل نیاز دارد، حساب جدا بساز (بخش «کاربران» یا «مدیریت نقش‌ها»)، نه اینکه رمز خودت را بدهی.
- **حالت sandbox (آزمایشی) زرین‌پال را روشن نگه ندار** بعد از اینکه فروشگاه واقعاً باز شد — در این حالت هیچ پول واقعی جابه‌جا نمی‌شود ولی مشتری فکر می‌کند خرید کرده.
- **اعتبار پیامک کاوه‌نگار را صفر نگه نکن.**
- **سفارش‌ها یا محصولات را از جای دیگری غیر از پنل تغییر نده.** هر ابزار یا نرم‌افزار دیگری که کسی پیشنهاد بدهد («برای سرعت بیشتر مستقیم توی دیتابیس عوضش کن» یا مشابه آن) را قبول نکن — تنها راه امن، همین پنل است.
- **اگر ایمیلی، پیامکی، یا تماسی درباره‌ی «مشکل فنی سایت» یا «آپدیت فوری» گرفتی که ازت رمز، کد پرداخت، یا دسترسی خواست** — پاسخ نده، با توسعه‌دهنده تماس بگیر. این روش رایج کلاهبرداری است.
- اگر چیزی عجیب دیدی (سفارش‌های تکراری، محصولات ناشناس، رفتار غیرمنتظره) به‌جای امتحان‌کردن راه‌حل‌های اینترنتی، همان لحظه با توسعه‌دهنده تماس بگیر.

---

## ۷. بک‌آپ (نسخه پشتیبان) کجاست

هر شب ساعت ۴ صبح (به وقت تهران)، یک نسخه کامل از دیتابیس و عکس‌ها به‌صورت خودکار گرفته می‌شود — نیازی به کار دستی از طرف تو نیست.

- نسخه‌ها هم روی خود سرور نگه داشته می‌شوند و هم (در صورت تنظیم‌بودن فضای ابری) در یک فضای ذخیره‌سازی جدا خارج از سرور — یعنی حتی اگر خود سرور کاملاً از دست برود، بک‌آپ‌ها جای دیگری هم هستند.
- ۷ نسخه‌ی روزانه‌ی اخیر و ۴ نسخه‌ی هفتگی همیشه نگه داشته می‌شوند.
- **این بخش کاملاً فنی است و نیازی نیست خودت با آن کار کنی** — فقط بدان که وجود دارد و خودکار است. اگر روزی نیاز به بازگردانی یک نسخه‌ی قدیمی بود (مثلاً یک اشتباه بزرگ رخ داد)، با توسعه‌دهنده تماس بگیر، نه اینکه خودت چیزی را دستکاری کنی.

---

اگر جایی از این راهنما گنگ بود یا با چیزی که در پنل می‌بینی فرق داشت، به‌جای حدس‌زدن، سؤال کن.
