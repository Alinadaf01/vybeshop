# یادداشت‌های تسک پایانی

این فایل هم‌زمان با پیشرفت کار به‌روزرسانی می‌شود. هر بخش شامل: چه شد، چه تصمیمی گرفته شد و چرا.

---

## بخش ۱ — آیکون‌های هدر ✅

- `lucide-react` نصب شد.
- `Header.tsx`: کامپوننت مشترک `HeaderIconButton` ساخته شد که هم به‌صورت لینک هم دکمه کار می‌کند — آیکون + برچسب مونو با `gap-2` (۸px) در دسکتاپ (`lg:`)، فقط آیکون با ناحیه لمسی ۴۴×۴۴ در موبایل.
- دکمه همبرگر بین `Menu` و `X` بر اساس state واقعی `menuOpen` (از `RootLayout` بالا می‌آید) جابه‌جا می‌شود — قبلاً `Header` فقط `onMenuOpen` می‌گرفت و از باز/بسته بودن خبر نداشت، به `menuOpen` + `onMenuToggle` تغییر کرد. همین تغییر در `PrerenderLayout.tsx` هم اعمال شد.
- **تصمیم خودم:** دکمه SEARCH در هدر و منوی موبایل قبلاً هیچ لینکی نداشت (نه در دسکتاپ نه موبایل) — چون صفحه `/search` از قبل وجود دارد و همین دکمه را دارم تغییر می‌دهم، آن را به `/search` وصل کردم. این خارج از دامنه دقیق تسک بود ولی رفع یک باگ بی‌ربط به آیکون نبود — همان المنت بود.
- **تصمیم خودم:** دکمه ACCOUNT قبلاً با `hidden sm:block` در موبایل کاملاً پنهان بود (چون متن «ACCOUNT» جا نمی‌شد). حالا که فقط آیکون است جا می‌شود، پس آن را در همه‌ی اندازه‌ها نمایان کردم — مطابق با «فقط آیکون در موبایل» نه «مخفی در موبایل».
- **باگ واقعی پیدا و رفع شد:** دکمه همبرگر `shrink-0` نداشت (قبل از این تسک هم همینطور بود) و در فلکس هدر روی موبایل عرضش زیر ۴۴px فشرده می‌شد (فقط ارتفاعش ۴۴px می‌ماند). با زنده تست در مرورگر (نه فقط build) کشف و با اضافه‌کردن `shrink-0` رفع شد.
- تست زنده: موبایل (۳۷۵px) و دسکتاپ (۱۴۴۰px) — آیکون‌ها، اندازه لمسی، برچسب دسکتاپ، toggle منو، بج شمارنده سبد — همه تأیید شد.
- `npm run check`: ۰ خطا.

## بخش ۲ — وسط‌چین کردن بلوک برند فوتر ✅

- در `Footer.tsx` به بلوک بالایی (لوگو + شعار) `items-center text-center` اضافه شد. ستون‌های زیرش دست‌نخورده ماندند (راست‌چین طبیعی RTL).
- تست زنده در دسکتاپ (۱۴۴۰px) — تأیید شد.
- `npm run check`: ۰ خطا.

## بخش ۴ — دسترسی ادمین به فاکتور مشتری ✅

(زودتر از بخش ۳ انجام شد چون مستقل بود؛ بخش ۳ نیاز به تحقیق گسترده‌تری داشت.)

- **تصمیم خودم — کتابخانه آیکون در پنل ادمین:** پنل ادمین از قبل آیکون‌های SVG دستی (سبک Heroicons outline، viewBox 0 0 24 24، strokeWidth 1.8) دارد، نه lucide-react. برای آیکون‌های تازه (این بخش + بخش ۳) به‌جای نوشتن دستی مسیرهای SVG از حافظه (ریسک واقعی خطا در path data)، `lucide-react` را در پنل ادمین هم نصب کردم — دقیقاً همان کتابخانه‌ای که برای فروشگاه استفاده شد و از قبل در مونوریپو تأیید شده است. آیکون‌های دستی قدیمی (سایدبار، هدر) دست‌نخورده ماندند؛ فقط آیکون‌های جدید از lucide می‌آیند. عدم یکدستی سبک یک مسئله زیبایی‌شناسی جزئی است در برابر ریسک آیکون خراب/نادرست.
- `admin/src/pages/orders/useOrderDocuments.ts` (جدید) — هوک مشترک برای دانلود فاکتور/برگه بسته‌بندی با state لودینگ و toast خطا، هم در صفحه جزئیات هم در منوی ردیف استفاده می‌شود.
- **معیار «پرداخت‌شده به بعد»:** به‌جای وایت‌لیست کردن status (که چون سفارش paid/processing می‌تواند بعداً canceled شود، نادرست می‌بود)، از `order.paidAt !== null` استفاده شد — درست‌ترین سیگنال، چون حتی سفارش لغوشده‌ای که قبلاً پرداخت شده هنوز فاکتور معتبر دارد.
- دکمه‌های «دانلود فاکتور» و «برگه بسته‌بندی» در `OrderDetailPage.tsx` (کنار Chip وضعیت).
- در `OrderRow.tsx`: چون هیچ کامپوننت منوی از‌قبل‌موجودی در پنل نبود، یک منوی کوچک (سه‌نقطه) با همان الگوی منوی پروفایل در `Header.tsx` ادمین ساخته شد.
- **باگ واقعی پیدا و رفع شد (زنده تست شد):** اولین پیاده‌سازی منو از `absolute` داخل `<td>` استفاده می‌کرد که چون جدول در `overflow-x-auto` است، منو به‌طور نامرئی بریده می‌شد. با `createPortal` به `document.body` + محاسبه موقعیت با `getBoundingClientRect()` حل شد. باگ دوم: چون ستون منو در RTL سمت چپ جدول (لبه صفحه) است، چیدن منو با `right - width` آن را به مختصات منفی (بیرون از صفحه) می‌برد؛ منطق چرخش به چیدن از چپ دکمه اضافه شد وقتی جا کم است.
- تست زنده کامل: سفارش پرداخت‌نشده (دکمه‌ها نیستند) در برابر سفارش پرداخت‌شده (دکمه‌ها هستند)، دانلود واقعی از صفحه جزئیات (`GET /orders/1/invoice.pdf → 200`) و از منوی ردیف — هر دو از همان اندپوینتی که فاکتور مشتری را می‌سازد.
- `npm run check` (ادمین): ۰ خطا.

## بخش ۳ — آیکون حالت‌های خالی/خطا ✅

- **فروشگاه:** `src/components/ui/EmptyState.tsx` یک prop اختیاری `icon?: LucideIcon` گرفت (پیش‌فرض `Inbox`، ۴۸px، `strokeWidth 1.5`). `ErrorState.tsx` چون همیشه یک معنای واحد دارد («خطا در دریافت اطلاعات»)، به‌جای prop، آیکون ثابت `AlertCircle` گرفت — تصمیم: تنوع per-context فقط برای EmptyState معنا دارد چون «خالی بودن» بسته به لیست فرق می‌کند، ولی «خطا» همیشه یک شکل است.
  - صفحات به‌روزشده با آیکون مناسب زمینه: `CartPage` (سبد خالی → `ShoppingBag`)، `ProductsPage` (بدون نتیجه → `SearchX`)، `BlogListPage` (بدون مطلب → `FileText`)، `AccountPage` (سفارش‌ها → `PackageOpen`/`AlertCircle`، آدرس‌ها → `MapPin`/`AlertCircle`، علاقه‌مندی‌ها → `Heart`/`AlertCircle`)، `CheckoutPage` (خطاهای دریافت آدرس/ارسال/درگاه → `AlertCircle`، هشدار نبود درگاه فعال → `AlertTriangle`).
- **پنل ادمین:** `Stateviews.tsx` مشابه فروشگاه — `EmptyState` با `icon?: LucideIcon` (پیش‌فرض `Archive`، جایگزین SVG دستی قدیمی)، `ErrorState` دست‌نخورده ماند (از قبل آیکون هشدار مناسبی داشت که در دامنه‌ی «فقط empty state» این بخش نبود).
  - همه‌ی ۲۱ محل استفاده از `<EmptyState>` در پنل با آیکون متناظر: سفارش‌ها→`ShoppingCart`، محصولات→`Package`، دسته‌بندی‌ها→`FolderTree`، مشخصات→`ListChecks`، قیمت‌گذاری→`Tag`، تاریخچه قیمت→`History`، موجودی→`Warehouse`، کاردکس→`ClipboardList`، کاربران→`Users`، پیام‌ها→`Mail`، نقش‌ها→`Shield`، مرجوعی‌ها→`RotateCcw`، نظرات→`Star`، کوپن‌ها→`Ticket`، بلاگ→`FileText`، سرچ کنسول (متصل‌نشده)→`Search`، روش‌های ارسال→`Truck`، کلیدهای API→`KeyRound`، و سه کارت گزارش (پرفروش‌ترین‌ها→`BarChart3`، به‌تفکیک دسته→`PieChart`، به‌تفکیک درگاه→`CreditCard`).
- `lucide-react` (که در بخش ۴ به ادمین اضافه شد) اینجا هم استفاده شد — همان تصمیم قبلی.
- تست زنده: با فیلتر جستجوی بی‌معنی روی `/products` حالت خالی با آیکون `Package` تأیید شد (رنگ سیان برند، ۲۸px، بدون کرش). صفحات دیگر با داده واقعی هم بدون خطای import/رندر بالا آمدند (خطای import سطح ماژول بلافاصله کل صفحه را می‌شکند، پس این تست غیرمستقیم هم برای بقیه‌ی ۲۰ صفحه معتبر است).
- `npm run check` (فروشگاه، شامل build کامل + prerender): ۰ خطا.
- `npm run check` (ادمین، شامل build): ۰ خطا؛ یک خطای TS «ShoppingCart declared but never used» در حین کار پیدا و بلافاصله رفع شد (آیکون در JSX جا افتاده بود).

## بخش ۵ — بازطراحی کامل ۹ سند PDF ⭐ (مهم‌ترین بخش) ✅

- **پالت رنگ:** یک بلوک `:root` واحد در `base.html` با دقیقاً پنج رنگ مشخص‌شده در تسک (گرافیت/مه‌آلود/تیتانیوم/خاکستری‌روشن/سیان) — هیچ رنگ دیگری در هیچ‌کدام از ۹ سند استفاده نشد؛ بدون سایه، بدون گرادیان.
- **لوگو — تصمیم مهم خودم:** به‌جای رندر تصویر آپلودی `SiteSettings.logo_light` (که رنگش خارج از کنترل ماست و می‌توانست پالت پنج‌رنگی را نقض کند)، از همان SVG وردمارک تخت `public/brand/vybe-wordmark.svg` استفاده شد که در فروشگاه هم برای `VybeWordmark.tsx` استفاده می‌شود — یک مسیر واحد با `fill="currentColor"`، این‌بار مستقیماً inline در تمپلیت جنگو با `mark_safe` و رنگ‌آمیزی با CSS `color: var(--graphite)`. این هم دقیقاً «لوگوی تخت» خواسته‌شده در تسک است، هم تضمین می‌کند رنگ همیشه در پالت مجاز بماند. فایل `apps/documents/pdf.py`: `vybe_wordmark_svg()` (با `lru_cache`).
- **هدر صفحه:** گرید سه‌ستونه — لوگو (راست، ۱۲mm ارتفاع) / نام سند (وسط، ۲۴pt) / شماره سند و تاریخ (چپ، مونو). زیرش خط ۱px خاکستری‌روشن + نوار ۲px سیان (تنها استفاده‌ی مجاز رنگ تاکیدی، دقیقاً یک‌بار در هر سند — تصمیم خودم برای اینکه این بودجه‌ی رنگی کجا خرج شود).
- **فوتر تکرارشونده در هر صفحه — تصمیم فنی مهم:** به‌جای تلاش برای شبیه‌سازی فوتر با CSS معمولی (که فقط یک‌بار در جریان صفحه رندر می‌شود، نه در هر صفحه‌ی چاپی)، از مکانیزم بومی Playwright (`display_header_footer` + `footer_template` با کلاس‌های `pageNumber`/`totalPages`) استفاده شد — تنها راه قابل‌اتکا برای فوتر واقعاً تکرارشونده با شماره صفحه‌ی زنده. این تمپلیت در یک سند ایزوله جدا از صفحه‌ی اصلی اجرا می‌شود (فونت‌ها و رنگ تیتانیوم دوباره در آن تعریف شدند، چون CSS صفحه‌ی اصلی به آن ارث نمی‌رسد).
- **حاشیه صفحه ۱۸mm در هر چهار طرف** — دقیقاً طبق مشخصات؛ قبلاً ۱۲-۱۶mm نامتقارن بود.
- **جدول‌ها:** هدر گرافیت/سفید/۶۰۰وزن، ردیف‌های زوج مه‌آلود (`nth-child(even)`)، خط جداکننده ۱px خاکستری‌روشن، بدون کادر بیرونی، کلاس `.num` برای ستون‌های عددی/مونو — که عمداً `text-align:start` (یعنی راست در RTL) است، نه چپ: یک اشتباه رایج «عادت فاکتور LTR» که این کلاس صراحتاً از آن جلوگیری می‌کند. `<thead>` به‌صورت طبیعی (بدون CSS اضافه) در صفحات بعدی تکرار می‌شود — رفتار پیش‌فرض موتور چاپ Chromium روی عنصر معنایی `<thead>`.
- **بازطراحی فاکتور (سند اصلی):** بلوک‌بندی دقیقاً طبق ترتیب خواسته‌شده — فروشنده/خریدار دوستونه → نوار اطلاعات سفارش (شماره فاکتور، شماره سفارش، تاریخ جلالی، روش/درگاه پرداخت، کد رهگیری تراکنش) → جدول اقلام → بلوک جمع‌بندی (راست‌چین در نیمه‌ی چپ صفحه، با `justify-content:flex-end` که در RTL یعنی «چپ») → مبلغ به حروف → کد رهگیری پستی (فقط اگر ارسال‌شده) → تگ‌لاین کم‌رنگ پایانی «طراحی‌شده برای ارتقای روزمرگی.» بدون کادر تزئینی، واترمارک یا تبلیغ.
- **تصمیم خودم — شماره فاکتور:** این اپ سیستم شماره‌گذاری جداگانه‌ای برای فاکتور ندارد؛ به‌جای ساختن یک سکانس تازه فقط برای نمایش، شماره سفارش هم به‌عنوان شماره فاکتور استفاده شد (هر دو label در نوار اطلاعات یک مقدار را نشان می‌دهند) — ساده‌سازی آگاهانه.
- **برگه بسته‌بندی:** بدون قیمت، فونت بزرگ‌تر (جدول ۱۱pt به‌جای ۹pt استاندارد)، تیک‌باکس (☐) کنار هر ردیف — طبق قبل، فقط با کلاس‌های برند جدید.
- **لیست ارسال روزانه:** فشرده، یک ردیف به ازای هر سفارش، ستون کد رهگیری خالی (برای نوشتن دستی در باجه پست).
- **گردش کالا (بازه) و صورت موجودی انبار:** بازطراحی با کلاس‌های مشترک؛ صورت موجودی همان ستون خالی «شمارش دستی» و «مغایرت» را برای شمارش فیزیکی حفظ کرد.
- **گزارش فروش دوره‌ای — تصمیم خودم:** تسک اجازه داد «نمودار ساده اگر شدنی است، وگرنه فقط جدول». چون نمودار معمولاً برای تفکیک بصری به چند رنگ نیاز دارد و این مستقیماً با محدودیت «فقط پنج رنگ مشخص» تناقض دارد، فقط جدول (بدون نمودار) انتخاب شد — امن‌ترین گزینه برای رعایت پالت.
- **لیست قیمت — گروه‌بندی بر اساس دسته‌بندی:** `price_list.py` بازنویسی شد تا محصولات را بر اساس نام دسته‌بندی گروه کند (`itertools.groupby`) به‌جای یک جدول تخت — مناسب مشتریان عمده طبق خواسته تسک.
- **بررسی واقعی (نه فقط تست عدم خطا):** هر ۸ سند (فاکتور مشترک با نسخه ادمین = سند نهم) با داده‌ی واقعی seed شده تولید شد، به PNG تبدیل شد (با نصب موقت `pymupdf` در venv — فقط ابزار بازبینی محلی، به `requirements.txt` اضافه نشد) و همه با چشم بررسی شدند: شکل‌گیری صحیح فارسی، راست‌چین بودن اعداد مونو، صفحه‌بندی چندصفحه‌ای صحیح (گردش کالا ۵ صفحه، صورت موجودی و لیست قیمت ۲ صفحه — `<thead>` در همه‌ی صفحات بعدی درست تکرار شد)، بدون سرریز از لبه. یک نگرانی اولیه (وردمارک VYBE در پیش‌نمایش «آینه‌ای»/غیرمعمول به نظر رسید) با رندر مجزای همان فایل SVG بیرون از هرگونه کانتکست RTL بررسی و رفع نگرانی شد — طرح اصلی و عمدی همین وردمارک برند است (همان فایلی که در فوتر/هدر فروشگاه هم استفاده می‌شود)، نه باگ رندر.
- تست بک‌اند: `apps.admin_api.tests.test_documents` + `apps.orders` (۶۴ تست PDF/سفارش) پاس شد؛ کل مجموعه تست بک‌اند هم اجرا شد.
- `context.py`: `base_context()` دو پارامتر جدید گرفت — `doc_number`/`doc_date` (پیش‌فرض به تاریخ تولید سند) — برای هدر سه‌ناحیه‌ای؛ `logo_uri`/`file_uri` حذف شدند (دیگر استفاده نمی‌شوند).

## بخش ۶ — بازطراحی خروجی‌های اکسل ✅

- **فقط دو خروجی اکسل در کل اپ وجود داشت** (نه بیشتر): کاردکس انبار (`/stock-movements/export/`) و گزارش فروش دوره‌ای (`/reports/sales/export/`) — با جستجوی کامل در بک‌اند و فرانت ادمین (`grep openpyxl` + `grep .xlsx` روی `admin/src/lib/api.ts`) تأیید شد قبل از شروع کار، تا بخشی از‌قلم نیفتد.
- `apps/documents/excel.py` (جدید) — یک `build_workbook()` مشترک ساخته شد که هر دو خروجی از آن استفاده می‌کنند، تا استایل هرکدام دستی و ناهماهنگ نباشد: شیت RTL (`sheet_view.rightToLeft = True`)، ردیف هدر گرافیت/سفید/بولد/ارتفاع ۲۴، پنجره فریزشده زیر هدر، عرض ستون بر اساس محتوا (حداقل ۱۰ حداکثر ۴۵ کاراکتر)، آتوفیلتر روی محدوده‌ی هدر تا آخرین ردیف داده، ردیف اطلاعات بالای جدول (نام گزارش + فیلترهای اعمال‌شده + نام تهیه‌کننده + تاریخ تولید، همه در یک ردیف طبق متن دقیق تسک «یک ردیف اطلاعات»)، ردیف جمع کل بولد برای گزارش‌های عددی.
- **تصمیم خودم — فرمت عدد:** تسک گفته بود «فرمت عدد `#,##0` با پسوند تومان»، ولی برخی ستون‌های عددی (تعداد، مانده انبار) واحد شمارشی هستند نه پولی — گذاشتن «تومان» روی تعداد کالا غلط می‌بود. کلاس `Column` دو نوع فرمت جدا گرفت: `COUNT_FORMAT` (`#,##0`، بدون پسوند، برای شمارش) و `TOMAN_FORMAT` (`#,##0" تومان"`، فقط برای مبالغ پولی واقعی).
- **تصمیم خودم — فونت:** برخلاف PDF که فونت را embed می‌کند، اکسل فقط اسم فونت را ذخیره می‌کند و اکسل کاربر نهایی خودش فونت را resolve می‌کند — روی سیستمی که Peyda نصب نیست به‌جایش فونت جایگزین می‌آید (بی‌ضرر) یا اگر تصادفاً فونتی هم‌نام نصب باشد ممکن است غلط رندر شود. پس فونت را دست‌نخورده به پیش‌فرض اکسل گذاشتیم؛ این بخش از تسک صراحتاً فقط برای PDF معنا دارد نه اکسل.
- تاریخ‌ها به‌صورت رشته‌ی شمسی (نه نوع تاریخ بومی اکسل، چون تقویم شمسی نوع بومی اکسل نیست) با همان `format_jalali_date` مشترک با PDF.
- هر دو خروجی از قبل هم فیلترهای فعال جدول را اعمال می‌کردند (بررسی شد، نیازی به تغییر رفتار نبود) — فقط استایل و ساختار بازطراحی شد.
- **بررسی واقعی:** هر دو فایل با داده واقعی تولید و دوباره با `openpyxl` باز شدند تا مقادیر واقعی خواص (rightToLeft، رنگ fill هدر، فونت، ارتفاع ردیف، freeze_panes، محدوده autofilter، فرمت عدد هر ستون، مقدار و بولد بودن ردیف جمع کل) به‌صورت برنامه‌ای بررسی شود — نه فقط اینکه فایل بدون خطا تولید شود. LibreOffice برای رندر تصویری روی این سیستم نصب نیست، پس این بررسی ساختاری (که برای صحت اکسل حتی دقیق‌تر از یک اسکرین‌شات است) جایگزین شد.
- تست بک‌اند: `apps.admin_api.tests.test_inventory` + `test_reports` (۱۴ تست، شامل تست‌های موجود این دو اندپوینت) پاس شد؛ چون تست‌های موجود فقط content-type و رفتار HTTP را چک می‌کردند نه محتوای دقیق سلول‌ها، با بازطراحی هم پاس ماندند.
- `apps/documents/responses.py`: دو تابع کمکی جدید `xlsx_filename`/`xlsx_response` اضافه شد، هم‌الگو با `pdf_filename`/`pdf_response` موجود.
- `openpyxl` از قبل در `requirements.txt` بود؛ وابستگی جدیدی اضافه نشد.

## بخش ۷.۱ — پیش‌رندر خانه/بلاگ + دی‌هایدریت کوئری ✅

- **نکته معماری مهم که قبل از شروع کشف شد:** `main.tsx` فروشگاه از `createRoot().render()` استفاده می‌کند، نه `hydrateRoot()` — یعنی سمت کلاینت یک رندر CSR کاملاً تازه است، نه هیدریشن واقعی DOM (این را کامنت خود `PrerenderLayout.tsx` هم صراحتاً می‌گفت). پس مشکل واقعی که این بخش باید حل کند دقیقاً «فلش/رفچ» است، نه «میسمچ هیدریشن» — و راه‌حل درست همان چیزی است که تسک گفته: کش React Query را دی‌هایدریت کن، نه بازنویسی کل استراتژی رندر (که ریسک و دامنه‌ی بسیار بزرگ‌تری دارد و خارج از این بخش است).
- `src/entry-server.tsx`: علاوه بر ۴ صفحه‌ی قبلی (about/contact/catalog/categories)، `HomePage` و `BlogListPage` هم به `BODY_PAGES` اضافه شدند. یک `ROUTE_PREFETCHES` مپ جدید کوئری‌های اضافی هر مسیر را (فراتر از site-settings مشترک همه‌ی صفحات) قبل از `renderToStaticMarkup` پیش‌واکشی می‌کند: صفحه اصلی → محصولات/دسته‌بندی‌ها/۳ پست بلاگ اخیر؛ بلاگ → لیست پست‌ها (بدون فیلتر، صفحه ۱).
- `dehydrate(queryClient)` بعد از پیش‌واکشی فراخوانی و در `PrerenderResult` برگردانده می‌شود.
- `scripts/prerender.mjs`: خروجی دی‌هایدریت‌شده به‌صورت `<script>window.__REACT_QUERY_STATE__=...</script>` قبل از `</body>` تزریق می‌شود (با همان تابع escape موجود برای JSON-LD، تا امن باشد).
- `src/main.tsx`: قبل از mount، اگر `window.__REACT_QUERY_STATE__` وجود داشت با `hydrate(queryClient, ...)` به کش کلاینت منتقل و سپس پاک می‌شود.
- **باگ واقعی پیدا و رفع شد (قبل از اینکه به تست برسد، با فکر کردن روی رفتار پیش‌فرض React Query v5):** فقط `hydrate()` کردن کافی نیست — بدون `staleTime`، React Query داده‌ی هیدریت‌شده را فوراً «بیات» می‌داند و بلافاصله بعد از mount دوباره fetch می‌کند (داده کش‌شده را نشان می‌دهد، پس فلش بصری نیست، ولی همان رفچ اضافه‌ای که تسک صراحتاً گفته «تا هیدریشن دوباره فچ نکند» رخ می‌دهد). برای کوئری‌های دقیقاً همان‌هایی که در entry-server پیش‌واکشی می‌شوند (`products/home`، `categories`، `blogPosts/home` در `HomePage.tsx`؛ `blog-posts` در `BlogListPage.tsx`) مقدار `staleTime: 60_000` اضافه شد.
- **بررسی واقعی:** با `npm run preview` (سرور واقعی روی خروجی `dist/`، نه dev server) صفحه‌ی اصلی و `/blog` به‌صورت مستقیم (full navigation، نه SPA transition) بارگذاری شدند و تب Network بررسی شد — هیچ درخواست API (`/api/products`، `/api/categories`، `/api/blog-posts`) دیده نشد، فقط فونت/تصویر/JS/CSS استاتیک؛ یعنی هر دو کوئری واقعاً از کش هیدریت‌شده resolve شدند، نه یک فچ نامرئی پس‌زمینه. محتوای واقعی (نام محصولات، قیمت‌ها، عنوان پست‌های بلاگ) هم در HTML تولیدشده و هم در اسکرین‌شات زنده تأیید شد.
- `npm run check` (typecheck + lint + build + prerender): ۰ خطا؛ `prerender: 6 full-body pages` (قبلاً ۴ تا).
- **یادداشت جانبی:** `npm run test` (vitest) از قبل از این بخش خراب بود — پکیج `vitest` نصب نیست ولی در `package.json` اسکریپتش هست؛ این مربوط به این بخش نبود، برای بخش ۷.۳ (پاکسازی) یادداشت شد.

## بخش ۷.۲ — اصلاح تناقض جنس محصولات ✅

- همه‌ی ۲۵ محصول در `src/data/products.ts` بررسی شدند. قبل از اصلاح: ۱۲ محصول «پلی‌کربنات مقاوم»، ۸ محصول «آلومینیوم CNC شده»، ۲ محصول «نایلون تقویت‌شده با فایبر کربن»، فقط ۳ محصول واقعاً TPU.
- **تصمیم خودم — نگاشت جنس‌ها:** به‌جای یک نگاشت یکنواخت (مثلاً همه به PETG)، بر اساس کارکرد واقعی هر محصول تصمیم گرفته شد تا کاتالوگ همچنان منطقی به نظر برسد:
  - **PLA+ مات** (۱۲ محصول): قطعات سبک و داخلی با کاربرد کم‌فشار — پایه Air، داک‌های رومیزی، قلاب‌های سبک، کل خانواده Grid، کیف کارت مغناطیسی، اکسسوری‌های کوچک.
  - **PETG** (۱۰ محصول): قطعاتی که به مقاومت بیشتر نیاز دارند — مفصل‌های قابل‌تنظیم (Stand Pro، Fold Desk/Travel/Slim/Tripod)، قطعات در معرض گرما/رطوبت خودرو یا موتور (Clip Car، Snap Mag، Snap Car)، قلاب سنگین ۵ کیلویی (Hook Wall)، داک پرو.
  - **TPU** (۳ محصول، بدون تغییر): Clip Bike و Clip Moto (جذب ارتعاش)، Clip Cable — این‌ها از قبل هم درست بودند.
- **متن توضیحات هم اصلاح شد، نه فقط فیلد `material`** — چون تناقض فقط در فیلد نبود:
  - `VYBE Stand Pro`: «از یک بلوک آلومینیوم CNC‌شده ساخته شده» → «با بدنه‌ی PETG چاپ‌شده» (هم `shortDescription` هم `description`).
  - `VYBE Fold Desk`: «لولای فلزی داخلی» → «لولای اصطکاکی PETG داخلی».
  - `VYBE Fold Tripod`: «سه پایه فلزی قابل‌جمع‌شدن» → «سه پایه تاشوی سبک» (حذف کامل ادعای فلز بودن پایه‌ها).
  - `VYBE Hook Wall`: «با پایه فلزی داخلی که تا ۵ کیلوگرم را تحمل می‌کند» → «با پیچ‌های فلزی تقویتی تا ۵ کیلوگرم را تحمل می‌کند» — طبق اجازه صریح تسک، پیچ به‌عنوان قطعه اکسسوری فلزی باقی ماند، فقط دیگر به‌عنوان «بدنه/پایه» توصیف نمی‌شود.
  - دو مورد باقی‌مانده که «فلزی» در آن‌ها هست (`Clip Moto`: «بست فلزی تقویت‌شده»، همان `Hook Wall`) عمداً دست‌نخورده ماندند چون از قبل هم درست توصیف شده بودند — قطعه سخت‌افزاری کوچک، نه جنس بدنه.
- **بک‌اند:** `backend/apps/catalog/management/commands/seed_storefront.py` مستقیماً دست نخورد چون داده‌اش را از `backend/fixtures/*.json` می‌خواند، و آن فایل‌ها خودشان با `node scripts/export-fixtures.mjs` مستقیماً از `src/data/*.ts` تولید می‌شوند (نه یک کپی دستی جدا). فقط اسکریپت را دوباره اجرا کردم — `products.json` با جنس‌های اصلاح‌شده بازتولید شد، بقیه فیکسچرها بدون تغییر ماندند (با `git diff --stat` تأیید شد).
- **بررسی واقعی:** `python manage.py seed_storefront` روی دیتابیس dev دوباره اجرا شد و با کوئری مستقیم روی مدل `Product` تأیید شد که مقدار `material` در پایگاه‌داده واقعی هم فقط سه مقدار مجاز است (`PLA+ مات` / `PETG` / `TPU انعطاف‌پذیر`؛ رشته خالی هم دیده شد که مربوط به محصولات تستی/دیباگ خارج از seed است، نه بخشی از این باگ — برای بخش ۷.۳ یادداشت شد).
- `grep` نهایی روی کل `products.ts` برای «آلومینیوم»، «نایلون»، «فایبر کربن»، «پلی‌کربنات»، «CNC» صفر نتیجه داد.
- `npm run check` (شامل build کامل + prerender): ۰ خطا.
