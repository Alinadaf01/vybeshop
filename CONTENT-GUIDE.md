# راهنمای تحویل متن و عکس — VYBE

این سند برای علی است. هدف: بدانی چه چیزی را کجا تحویل بدهی تا بدون رفت‌وبرگشت وارد سایت شود.

---

## بخش ۱ — تفکیک ایستا از پویا

سایت دو نوع محتوا دارد و مسیرشان کاملاً فرق می‌کند.

### 🅰️ ایستا — تو می‌نویسی، در کد می‌نشیند

متن‌هایی که ثابت‌اند و از پنل ادمین تغییر نمی‌کنند. اینها در `src/content/` زندگی می‌کنند.

| فایل | چه چیزی داخلش است |
|---|---|
| `home.ts` | تیتر هیرو، شعار، عنوان سکشن‌ها، سه مرحله فرایند، متن جامعه |
| `about.ts` | کل روایت صفحه درباره ما |
| `catalog.ts` | متن صفحه کاتالوگ (نه خود فایل PDF) |
| `contact.ts` | برچسب فرم، پیام‌های خطا، سؤالات متداول |
| `categories.ts` `products.ts` `blog.ts` | فقط عنوان و برچسب صفحه، نه خود داده |

### 🅱️ پویا — از بک‌اند می‌آید

اینها هرگز نباید در کد هاردکد شوند. الان با داده فیک کار می‌کنند و بعداً از جنگو می‌آیند.

| داده | منبع نهایی |
|---|---|
| محصولات: نام، توضیح، مشخصات فنی، قیمت، موجودی، عکس | `GET /api/products/` |
| دسته‌بندی‌ها: نام، توضیح، تعداد | `GET /api/categories/` |
| مقالات بلاگ | `GET /api/posts/` |
| **تنظیمات سایت:** تلفن، ایمیل، آدرس، ساعات کاری، شبکه‌های اجتماعی، نماد اعتماد، درگاه پرداخت | `GET /api/settings/` |
| فایل PDF کاتالوگ + متادیتا (حجم، تعداد صفحه، تاریخ ویرایش) | `GET /api/catalog/` |

> ⚠️ **تنظیمات سایت الان در `contact.ts` و فوتر هاردکد است.** باید به یک لایه `siteSettings` منتقل شود که امضایش دقیقاً همان چیزی باشد که جنگو بعداً می‌دهد. این در تسک فعلی Code آمده.

**چیزی که تو باید بنویسی:** فقط ستون 🅰️.
**چیزی که تو باید در پنل ادمین وارد کنی (بعداً):** ستون 🅱️.

---

## بخش ۲ — چطور متن‌ها را تحویل بده

یک فایل متنی ساده بساز (Word یا txt یا همین‌جا در چت) و **دقیقاً از این سرفصل‌ها استفاده کن**. هر جا متنی نداری، بنویس «فعلاً بماند» تا نسخه موجود دست نخورد.

```
═══════════════════════════════
صفحه هوم
═══════════════════════════════

[هیرو]
تیتر:
زیرتیتر:
متن دکمه:
کپشن فنی زیر هیرو:        (مثال فعلی: PLA · FDM · 0.2MM LAYER)

[نوار جوهره]
جمله وسط‌چین:             (فعلی: طراحی مینیمال. حداکثر کاربرد.)

[سکشن چگونه ساخته می‌شود]
عنوان:
زیرعنوان:
مرحله ۱ — عنوان / متن:
مرحله ۲ — عنوان / متن:
مرحله ۳ — عنوان / متن:
تگ‌های فنی:               (فعلی: PLA، FDM، 0.2MM LAYER، BAMBU LAB P1S)

[عنوان سکشن‌ها]
دسته‌بندی‌ها:
اکنون موجود است:
از بلاگ:

[جامعه]
عنوان:
متن:
متن دکمه عضویت:


═══════════════════════════════
صفحه درباره ما
═══════════════════════════════

[هیرو]
برچسب بالا:               (فعلی: ABOUT VYBE · WORKSHOP, TEHRAN)
تیتر:
متن معرفی:

[چهار عدد کلیدی]
۱ — برچسب / عدد:          (فعلی: FOUNDED / 1401)
۲ — برچسب / عدد:
۳ — برچسب / عدد:
۴ — برچسب / عدد:

[چرا VYBE وجود دارد]
عنوان:
پاراگراف ۱:
پاراگراف ۲:

[پنج اصل طراحی]
اصل ۱ — عنوان / توضیح:
اصل ۲ — عنوان / توضیح:
اصل ۳ — عنوان / توضیح:
اصل ۴ — عنوان / توضیح:
اصل ۵ — عنوان / توضیح:

[فرایند تولید]
عنوان:
متن:
مشخصات کارگاه (پرینتر / متریال / لایه / نرخ دورریز):
پنج مرحله — عنوان و متن هرکدام:

[ارزش‌ها]
عنوان:
ارزش ۱ — عنوان / متن:
ارزش ۲ — عنوان / متن:
ارزش ۳ — عنوان / متن:

[کارگاه]
عنوان:
متن:

[CTA پایانی]
تیتر:
متن دکمه‌ها:


═══════════════════════════════
صفحه کاتالوگ
═══════════════════════════════

تیتر:
توضیح یک‌خطی:
متن دکمه دانلود:
سکشن «داخلش چیست» — سه یا چهار مورد با عنوان و توضیح:
متن CTA پایانی:


═══════════════════════════════
صفحه تماس با ما
═══════════════════════════════

تیتر صفحه:
توضیح بالای فرم:
سؤالات متداول — هر سؤال و جواب:

⚠️ تلفن، ایمیل، آدرس و ساعات کاری را اینجا ننویس — از بک‌اند می‌آیند.


═══════════════════════════════
فوتر
═══════════════════════════════

شعار زیر لوگو:            (فعلی: طراحی‌شده برای ارتقای روزمرگی.)
عنوان ستون‌ها:
متن خبرنامه:
```

---

## بخش ۳ — چطور عکس‌ها را تحویل بده

### قاعده ساده

**نام فایل تعیین می‌کند عکس کجا می‌نشیند.** اگر نام درست باشد، هیچ توضیحی لازم نیست.

یک پوشه به اسم `images` بساز، داخلش این ساختار را بزن و عکس‌ها را با همین نام‌ها بگذار:

```
images/
├── products/
│   ├── vybe-stand-pro/
│   │   ├── 1.jpg          ← عکس اصلی (در کارت محصول و OG استفاده می‌شود)
│   │   ├── 2.jpg
│   │   └── 3.jpg
│   ├── vybe-dock-mini/
│   │   ├── 1.jpg
│   │   └── 2.jpg
│   └── ...
├── categories/
│   ├── desktop-stands.jpg
│   ├── charging-docks.jpg
│   ├── phone-mounts.jpg
│   ├── wall-hooks.jpg
│   ├── modular-grid.jpg
│   ├── travel-folding.jpg
│   ├── magnetic-mounts.jpg
│   └── accessories.jpg
├── marketing/
│   ├── hero.jpg               ← عکس بزرگ صفحه اصلی
│   ├── macro-layer-lines.jpg  ← ماکرو خط لایه
│   └── workshop-wide.jpg      ← نمای کارگاه
├── blog/
│   └── <اسلاگ-مقاله>.jpg
└── catalog/
    ├── cover.jpg
    └── spread-01.jpg ... spread-06.jpg
```

### لیست کامل اسلاگ محصولات

هر پوشه باید **دقیقاً** یکی از این نام‌ها باشد:

```
vybe-stand-pro      vybe-stand-air      vybe-dock-pro       vybe-dock-mini
vybe-dock-duo       vybe-clip-cable     vybe-clip-car       vybe-clip-bike
vybe-clip-moto      vybe-hook-wall      vybe-hook-coat      vybe-hook-cable
vybe-hook-duo       vybe-grid-base      vybe-grid-panel     vybe-grid-tray
vybe-grid-connector vybe-fold-desk      vybe-fold-slim      vybe-fold-travel
vybe-fold-tripod    vybe-snap-mag       vybe-snap-car       vybe-snap-wallet
```

### مشخصات فنی عکس

| مورد | مقدار |
|---|---|
| فرمت تحویل | JPG یا PNG (تبدیل به WebP سمت ما انجام می‌شود) |
| عکس محصول | مربع یا ۴:۵ عمودی — **همه محصولات یک نسبت** |
| حداقل عرض | ۱۶۰۰ پیکسل |
| عکس دسته‌بندی و مارکتینگ | افقی ۱۶:۹ یا ۳:۲، حداقل ۲۰۰۰ پیکسل عرض |
| پس‌زمینه | سفید، خاکستری، بتن، چوب روشن یا مشکی مات |
| نور | طبیعی، سایه نرم |
| ممنوع | رنگ اشباع‌شده، پلاستیک براق، رندر تقلبی، فیلتر سنگین، لوازم اضافه در کادر |

### اگر عکس همه محصولات را نداری

**فهرست کن کدام‌ها را داری.** پیشنهاد من: کاتالوگ سایت را به همان تعداد محصولی که عکس واقعی دارد کم کنیم. بیست‌وچهار محصول با جای خالی، بدتر از هشت محصول با عکس واقعی است — و برند بوک هم می‌گوید «رندر تقلبی» ممنوع.

---

## بخش ۴ — یادداشت برای فاز بک‌اند

> 📌 **این را در شروع فاز بک‌اند به من یادآوری کن.**

قبل از صدور تسک بک‌اند، این سه چیز را تحویل بده تا تسک یک‌جا و کامل صادر شود، نه قابلیت‌به‌قابلیت:

۱. **قالب داشبورد ادمین با Tailwind** که پیدا کرده‌ای — خود فایل‌ها
۲. **فهرست قابلیت‌های هر بخش** از سیزده بخش پنل: داشبورد، محصولات، دسته‌بندی‌ها، مشخصات محصولات، پنل اصلاح قیمت، سفارشات، سرچ کنسول گوگل، مدیریت موجودی، کاربران، پیام‌ها، مدیریت فروش، گردش کاردکس کالا، تنظیمات سایت
۳. **قرارداد API فرانت** — که از روی همین پروژه استخراج می‌شود، تا مدل‌های جنگو دقیقاً همان شکلی باشند که فرانت انتظار دارد

با این سه، تسک بک‌اند یک‌بار و کامل صادر می‌شود.
