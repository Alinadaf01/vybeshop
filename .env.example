VITE_SITE_URL=https://vybeshop.ir

# Django API base URL. Leave unset to always use fake data (src/data/*.ts) —
# every function in src/lib/api.ts also falls back to fake data automatically
# if a real backend is configured but unreachable.
VITE_API_BASE_URL=http://localhost:8000/api
