# بک‌لاگ VYBE — کارهای معوق

هر چیزی که عمداً به تعویق افتاده، با دلیل و شرط شروعش. تا وقتی اینجاست، فراموش نشده.

---

## 🎬 پاس انیمیشن و حرکت — فاز محتوا

**وضعیت:** معوق به‌خواست کارفرما تا بعد از فاز بک‌اند.

فرانت فعلاً فقط `Reveal` با `IntersectionObserver` دارد (opacity + translateY). خشک به‌نظر می‌رسد چون هیچ تصویری هم لود نمی‌شود؛ بخشی از این خشکی با آمدن عکس‌ها خودبه‌خود حل می‌شود.

وقتی رسیدیم، این‌ها را پوشش بده:

- ورود محتوا هنگام اسکرول با تأخیر پلکانی (stagger) در گریدها
- ترنزیشن بین صفحات هنگام ناوبری
- اسکلتون واقعی هنگام لود، نه پرش ناگهانی محتوا
- حالت‌های ریزتعامل: هاور کارت محصول، تعویض تصویر گالری، افزودن به سبد
- شمارنده اعداد در صفحه درباره ما
- انتقال نرم تصویر از کارت محصول به صفحه جزئیات

**قید برند بوک:** «حرکت باید ظریف، آهسته، نرم و خطی باشد. هرگز پرزرق‌وبرق یا تهاجمی.»
توکن‌های `motion` در `design/tokens.json` از قبل تعریف شده‌اند: `ease` و `fast/base/slow`.
`prefers-reduced-motion` در همه موارد رعایت شود.

---

## 📝 محتوا و عکس واقعی

**وضعیت:** منتظر تحویل کارفرما. راهنما در `CONTENT-GUIDE.md`.

- متن‌های ایستا → `src/content/`
- عکس‌ها → `public/images/` طبق نام‌گذاری راهنما
- شش تصویر سکشن «جامعه VYBE» → `public/images/community/1..6.jpg`، مربع ۱۰۰۰×۱۰۰۰
- **داده محصولات با برند تضاد دارد:** ۸ محصول «آلومینیوم CNC» و ۱۱ «پلی‌کربنات»، هیچ‌کدام PLA نیستند. با آمدن محتوای واقعی اصلاح شود
- **بعد از آمدن عکس‌ها، پرفورمنس دوباره اندازه‌گیری شود.** اعداد فعلی (LCP ~۸۰ms، CLS صفر) بدون هیچ تصویری گرفته شده‌اند و قطعاً تغییر می‌کنند

---

## 🔍 SEO و رندر

- **پری‌رندر بدنه هوم و بلاگ** — در F6 عمداً کنار گذاشته شد چون `useQuery` نیاز به prefetch + dehydrate دارد. الان فقط چهار صفحه ثابت پری‌رندر می‌شوند
- **صفحات محصول** فعلاً CSR هستند. متادیتا در HTML اولیه هست (کافی برای تلگرام و اینستاگرام) ولی بدنه خالی است. **وقتی جنگو بالا آمد، تصمیم رندر سمت سرور گرفته شود**
- فایل PDF کاتالوگ فعلاً برند بوک است، نه کاتالوگ واقعی

---

## 🔄 صفحاتی که طراحی شده ولی به React تبدیل نشده

`design/12-cart.html` · `13-checkout.html` · `14-auth.html` · `15-account.html`

**شرط شروع:** هرکدام به فاز بک‌اند مربوطه‌اش گره خورده — تبدیلشان قبل از آن یعنی ساخت روی داده فیک و سیم‌کشی دوباره.

| صفحه | بعد از کدام فاز |
|---|---|
| `14-auth` · `15-account` | B3 (کاربران، OTP، JWT) |
| `12-cart` · `13-checkout` | B4 (سبد و سفارش) + B5 (پرداخت) |

**نکات اضافه‌شده هنگام تبدیل:**

- `13-checkout` با شش درگاه طراحی شده. لیست معتبر فقط چهار تاست (زرین‌پال، آیدی‌پی، اسنپ‌پی، دیجی‌پی) و **از `GET /api/payment-gateways/` می‌آید، نه هاردکد**. جزئیات در `design/HANDOFF.md`
- `15-account` باید دکمه **«دانلود فاکتور»** داشته باشد — هم در ردیف هر سفارش، هم در صفحه جزئیات. فقط برای سفارش‌های پرداخت‌شده

---

## 🐞 باگ‌های باز

_(فعلاً بدون مورد باز. آخرین مورد — فاصله عمودی بردکرامب — با `py-6` روی `<nav>` در `src/components/ui/Breadcrumb.tsx` رفع شد؛ کامیت `81ca98a`. همه صفحات استفاده‌کننده بررسی شدند و فاصله دوبرابر نشده.)_

---

## ⚠️ بدهی فنی و ریسک

- **لایسنس فونت پیدا** — محصول [فونت‌ایران](https://fontiran.com) است و استفاده تجاری لایسنس می‌خواهد. قبل از تحویل به کارفرما رسمی شود
- `npm audit` یک آسیب‌پذیری high در react-router (حالت RSC) نشان می‌دهد که به استفاده این پروژه ربطی ندارد. رصد شود
- superuser موقت توسعه `09120000000` — **قبل از هر دیپلوی production حذف شود**
- پورت Postgres محلی روی ۵۴۳۳ است نه ۵۴۳۲ (تداخل با نصب بومی ویندوز)

---

## ❌ رد شده — انجام نمی‌شود

- **پاس تغییر نام کلاس‌ها روی `design/00-06`** — آن فایل‌ها قبلاً به React تبدیل شده‌اند و حالا فقط مرجع تاریخی‌اند. تغییرشان هیچ چیزی در محصول عوض نمی‌کند
- **مدل `CommunityPost` در بک‌اند** — تصمیم شد شش تصویر جامعه ایستا و طراحی‌شده توسط کارفرما باشند، نه محتوای مشتری از پنل
