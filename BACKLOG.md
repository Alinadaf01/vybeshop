# بک‌لاگ VYBE — کارهای باقی‌مانده

آخرین به‌روزرسانی: قبل از دیپلوی نهایی

---

## 🔴 بلاکر لانچ — بدون این‌ها سایت واقعی نمی‌شود

این‌ها **از کارفرما** باید بیایند. هیچ‌کدام کار فنی ندارند و همه از پنل ادمین وارد می‌شوند.

| مورد | کجا وارد می‌شود | چرا لازم است |
|---|---|---|
| **اطلاعات فروشنده** — نام کسب‌وکار، کد اقتصادی، شناسه ملی | تنظیمات سایت ← اطلاعات سایت | بدون این، فاکتور PDF بدون مشخصات فروشنده صادر می‌شود |
| **تلفن، ایمیل، آدرس، ساعات کاری** | تنظیمات سایت | الان داده نمایشی است و در فوتر و صفحه تماس دیده می‌شود |
| **لینک شبکه‌های اجتماعی** | تنظیمات سایت | فوتر |
| **نماد اعتماد** — تصویر و کد | تنظیمات سایت | فوتر، اعتماد مشتری |
| **کلید کاوه‌نگار + شارژ** | تنظیمات ← کلیدهای API | **بدون این هیچ‌کس نمی‌تواند وارد شود** |
| **مرچنت زرین‌پال** (خارج از sandbox) | تنظیمات ← کلیدهای API | بدون این فروش انجام نمی‌شود |
| **کاتالوگ PDF واقعی** | تنظیمات ← کاتالوگ | الان برند بوک جایش گذاشته شده |
| **نام و مشخصات واقعی محصولات** | پنل ← محصولات | الان `محصول A` تا `I` و `دسته‌بندی ۱` تا `۸` است |

راهنمای کامل هر کدام در `SETUP-GUIDE.md`.

---

## 🟡 تصمیم‌های باز

### رندر سمت سرور صفحات محصول

صفحات محصول الان CSR هستند. متادیتا و JSON-LD در HTML اولیه هست (کافی برای پیش‌نمایش تلگرام و اینستاگرام و سیگنال گوگل) ولی **بدنه صفحه خالی است**.

این تصمیم عمداً به بعد از بالا آمدن جنگو موکول شد. حالا جنگو آماده است، پس **بعد از دیپلوی و با دیدن رفتار واقعی گوگل تصمیم بگیریم** — نه زودتر. اگر ایندکس شدن مشکل داشت، آن‌وقت SSR اضافه می‌کنیم.

### عکس محصولات

نُه عکس داریم برای نُه محصول، هر کدام یک تصویر. صفحه جزئیات گالری چندتصویری دارد که با یک عکس هم درست کار می‌کند، ولی وقتی محصولات واقعی آمدند، **دو تا سه عکس برای هر محصول** تجربه بهتری می‌دهد.

---

## ⚠️ بدهی فنی

| مورد | فوریت |
|---|---|
| **لایسنس فونت پیدا** — [فونت‌ایران](https://fontiran.com)، استفاده تجاری لایسنس می‌خواهد | قبل از تحویل رسمی به کارفرما |
| **حذف superuser توسعه `09120000000`** | در خود دیپلوی — در `DEPLOY-TASK.md` آمده |
| `npm audit` — آسیب‌پذیری high در react-router (حالت RSC که ما استفاده نمی‌کنیم) | رصد، فوری نیست |
| پورت Postgres محلی `5433` نه `5432` (تداخل با نصب بومی ویندوز) | فقط یادداشت محیط توسعه |
| **وابستگی PDF به Chromium/Playwright** — `cdn.playwright.dev` از IP ایران جیوبلاک است، بیلد ایمیج بک‌اند مجبور شد به GitHub Actions منتقل شود (`deploy/STAGE4-FIX-TASK.md`). بررسی شود که آیا `WeasyPrint` (پایتون خالص، بدون مرورگر، پشتیبانی RTL و جاسازی فونت) می‌تواند جایگزین Playwright شود — حدود ۲GB سبک‌تر و این جیوبلاک برای همیشه حذف می‌شود. **الان عوض نشود** — هر ۹ سند با Chromium بصری تأیید شده‌اند و تعویض موتور یعنی بازبینی کامل همه | **بعد از لانچ** |

---

## 📋 نکته‌های محیط توسعه

**پورت `4174` خروجی `vite preview` است، یعنی نسخه بیلدشده — نه کد فعلی.**
اگر تغییری دادی و ندیدی، اول `npm run build` بزن. چند بار سر همین وقت رفت.
برای دیدن تغییرات لحظه‌ای از `npm run dev` استفاده کن.

---

## ✅ تمام‌شده — دیگر باز نیست

- پاس انیمیشن و حرکت (ورود پلکانی، ریزتعامل‌ها، شمارنده اعداد، ترنزیشن صفحه)
- محتوا و عکس‌های کارفرما (متن هوم و درباره ما، نُه محصول، هشت دسته، شش کاشی جامعه، بلاگ)
- تبدیل چهار صفحه باقی‌مانده به React: سبد، چک‌اوت، ورود، حساب کاربری
- بازطراحی صفحه ورود با قالب OTP
- پری‌رندر هوم و بلاگ
- بخش «مدیریت صفحه اصلی» در پنل — هیرو، دو بلوک نمایش، کاشی‌های جامعه
- علاقه‌مندی‌ها (مدل، API، toggle، تب حساب، ادغام مهمان)
- مدیریت نقش‌ها و دسترسی + مدیریت رمز + ورود به‌جای کاربر
- لایه PDF — فاکتور مشتری + هشت سند ادمین، بازطراحی‌شده با برند
- خروجی اکسل بازطراحی‌شده
- آیکون‌های هدر و حالت‌های خالی/خطا
- rate limiting (که اصلاً وجود نداشت)
- **آدرس مطلق مدیا** — بدون این، همه تصاویر پنل‌محور بعد از دیپلوی شکسته می‌آمدند
- **باگ `display:contents` روی `<picture>`** — ریشه همه به‌هم‌ریختگی‌های چیدمان دوستونی

---

## ❌ رد شده

- تغییر نام کلاس‌ها در `design/00-06` — آن فایل‌ها فقط مرجع تاریخی‌اند
- مدل `CommunityPost` — شش تصویر جامعه ایستا و طراحی‌شده توسط کارفرما ماند
- نسخه بدون متن گرافیک‌های بازاریابی — کارفرما تصمیم گرفت همان‌ها با متن داخلی بمانند
