# تسک بک‌اند و پنل ادمین VYBE — Django + DRF + React Admin

**مخاطب:** Claude Code
**پیش‌نیاز خواندنی:** `API-CONTRACT.md` (قرارداد فرانت)، `admin-template/` (قالب پنل)، `design/tokens.json`، `design/HANDOFF.md`

---

## ۰. قواعد سخت — قبل از هر کاری بخوان

### الف) فرانت از قبل ساخته شده و قرارداد دارد

`API-CONTRACT.md` شکل دقیق پاسخ هر اندپوینتی است که فرانت صدا می‌زند. **این قرارداد تغییر نمی‌کند؛ بک‌اند باید با آن منطبق شود.** اگر جایی مدل دیتابیس با قرارداد نمی‌خواند، اول در `API-CONTRACT.md` مستند کن که چه تغییری لازم است و **بپرس** — خودسرانه شکل پاسخ را عوض نکن.

### ب) camelCase

فرانت `camelCase` انتظار دارد. `djangorestframework-camel-case` را نصب و به‌صورت سراسری فعال کن. مدل‌ها و کد پایتون `snake_case` بمانند.

### ج) قواعد داده

- قیمت: **عدد صحیح، تومان**، بدون اعشار
- تاریخ: ISO 8601 در API. تبدیل به شمسی فقط سمت نمایش
- کلید جستجوی جزئیات: **`slug`** نه `id`
- همه لیست‌ها با پاکت صفحه‌بندی `{count, next, previous, results}`
- دسته‌بندی‌ها آرایه ساده برمی‌گردند، **بدون** پاکت صفحه‌بندی

### د) امنیت — غیرقابل مذاکره

- **قیمت هرگز از کلاینت نمی‌آید.** کلاینت فقط `productId` و `quantity` می‌فرستد؛ سرور قیمت را محاسبه و در سفارش **snapshot** می‌کند
- سفارش فقط پس از `verify` سمت سرور `paid` می‌شود، نه با بازگشت کاربر از درگاه
- کال‌بک درگاه **idempotent** باشد؛ کلید یکتا روی تراکنش
- rate limit روی ارسال OTP، ورود، و فرم تماس
- کلیدهای API درگاه‌ها و کاوه‌نگار **رمزنگاری‌شده** در دیتابیس، هرگز در لاگ چاپ نشوند

---

## ۱. استک و اسکلت

```
Django 5 + Django REST Framework
PostgreSQL
djangorestframework-camel-case
djangorestframework-simplejwt      احراز هویت
django-filter                      فیلترها
Celery + Redis                     کارهای پس‌زمینه (پیامک، ایمیل، گزارش)
Pillow + django-imagekit           پردازش تصویر
drf-spectacular                    مستندسازی OpenAPI
```

ساختار: `/backend` کنار `/src` موجود، در همان ریپو. Docker Compose برای Postgres و Redis.

اپ‌های جنگو: `catalog` · `orders` · `inventory` · `users` · `content` · `settings` · `analytics`

---

## ۲. مدل‌های داده

### catalog

**`Category`** — `name`, `slug`, `description`, `image`, `parent` (FK به خودش، برای زیردسته), `order`, `isActive`
زیردسته‌بندی از طریق `parent` پیاده شود، حداکثر دو سطح.

**`Product`** — همه فیلدهای قرارداد به‌علاوه:
`sku`, `slug`, `name`, `shortDescription`, `description`, `price`, `category` (FK), `material`, `dimensions` (w/h/d)، `weight`, `layerHeight`, `inStock`, `stockCount`
**اضافه بر قرارداد:**
`order` (اولویت نمایش در سایت) · `isActive` (خاموش/روشن موقت) · `shippingTime` (زمان ارسال) · `returnPolicy` (وضعیت مرجوعی) · `productionStatus` (وضعیت ساخت) · `metaTitle` / `metaDescription` (SEO)

**`ProductImage`** — `product` (FK), `image`, `alt`, `order`
تعداد آزاد: از ۱ تا ۱۰+ تصویر به‌ازای هر محصول. تصویر با `order=1` تصویر اصلی و OG است.
هنگام آپلود، خودکار نسخه WebP و بندانگشتی ساخته شود.

**`ColorOption`** — `product` (FK), `name`, `hex`, `inStock`, `order`

---

### سیستم مشخصات پویا (بخش ۵ درخواست) — مهم‌ترین بخش فنی

الگوی EAV با سه مدل:

**`Attribute`** — `name` («سایز»), `slug`, `unit` («mm»), `inputType` (`select` | `text` | `number` | `boolean`), `categories` (M2M به Category), `isRequired`, `order`

**`AttributeValue`** — `attribute` (FK), `value` («M»), `order`
مقادیر از پیش تعریف‌شده برای حالت انتخابی.

**`ProductAttribute`** — `product` (FK), `attribute` (FK), `valueOption` (FK به AttributeValue، nullable), `valueText` (nullable)

**رفتار مورد نظر:** در فرم افزودن محصول، وقتی دسته‌بندی انتخاب می‌شود، فقط `Attribute`های همان دسته نمایش داده شوند. هر کدام یک dropdown از `AttributeValue`ها بدهند **به‌علاوه گزینه «مقدار دلخواه»** که یک فیلد متنی باز می‌کند. اگر ادمین مقدار دلخواه وارد کرد، اختیار داشته باشد آن را به لیست مقادیر آن attribute اضافه کند تا دفعه بعد در dropdown باشد.

> ⚠️ **نکته انطباق با قرارداد:** فرانت الان `material`، `dimensions`، `weight`، `layerHeight` را به‌عنوان فیلدهای ثابت انتظار دارد. آن‌ها را **ثابت نگه دار** چون UI مشخصی دارند. مشخصات پویا به‌صورت یک آرایه **اضافی** در پاسخ بیایند:
> ```ts
> specs: { label: string; value: string; unit?: string }[]
> ```
> این تغییر را در `API-CONTRACT.md` ثبت کن و صفحه جزئیات محصول را برای نمایشش به‌روز کن.

---

### orders

**`Cart`** — `user` (nullable), `sessionKey` (برای مهمان), `createdAt`
هنگام ورود کاربر، سبد مهمان با سبد کاربر **ادغام** شود.

**`CartItem`** — `cart`, `product`, `colorOption`, `quantity`

**`Order`** — `orderNumber` (یکتا، خوانا), `user`, `status`, `subtotal`, `discount`, `shippingCost`, `tax`, `total`, `shippingAddress` (snapshot، نه FK), `note`, `trackingCode`, `createdAt`, `paidAt`, `shippedAt`

ماشین حالت: `pending → paid → processing → shipped → delivered` با انشعاب `canceled` و `returned`.
تغییر وضعیت فقط از طریق متد مدل، نه ست کردن مستقیم فیلد. هر تغییر در `OrderStatusLog` ثبت شود.

**`OrderItem`** — `order`, `product` (FK), و **snapshot** از `productName`, `sku`, `price`, `colorName` در لحظه ثبت. اگر بعداً قیمت یا نام محصول عوض شد، سفارش قدیمی نباید تغییر کند.

**`Payment`** — `order`, `gateway`, `amount`, `authority`, `refId`, `status`, `rawResponse` (JSON), `idempotencyKey`, `createdAt`, `verifiedAt`

**`Return`** — `order`, `items`, `reason`, `status` (`requested → approved → received → refunded` / `rejected`), `adminNote`

---

### inventory

**`StockMovement`** (کاردکس — بخش ۱۳) — دفتر کل حرکت کالا:
`product`, `type` (`purchase` | `production` | `sale` | `return_in` | `adjustment` | `scrap`), `quantity` (مثبت یا منفی), `balanceAfter`, `reference` (شماره سفارش یا سند), `note`, `user`, `createdAt`

**قاعده حیاتی:** `Product.stockCount` هرگز مستقیم ویرایش نشود. **همیشه** از طریق ثبت یک `StockMovement` تغییر کند و مقدار محصول از مجموع حرکت‌ها محاسبه شود. این تنها راهی است که کاردکس با موجودی واقعی هم‌خوان بماند.

**`StockAlert`** — `product`, `reorderPoint`, `isActive`
وقتی موجودی زیر نقطه سفارش رفت، اعلان در داشبورد.

> **تفکیک بخش ۸ و ۱۳:** «مدیریت موجودی» وضعیت **فعلی** است (موجودی هر کالا، کالاهای رو به اتمام، نقطه سفارش، ارزش کل انبار). «گردش کاردکس» **تاریخچه** است (هر ورود و خروج با مانده بعد از آن، قابل فیلتر بر اساس بازه و نوع، خروجی اکسل).

---

### users

**`User`** — مدل سفارشی با `phone` به‌عنوان `USERNAME_FIELD`، به‌علاوه `firstName`, `lastName`, `email`, `isVerified`, `createdAt`

**`Address`** — `user`, `title`, `province`, `city`, `postalCode`, `line`, `receiverName`, `receiverPhone`, `isDefault`

**`OTPCode`** — `phone`, `code` (هش‌شده), `expiresAt`, `attempts`, `usedAt`
انقضا ۲ دقیقه، حداکثر ۵ تلاش، rate limit ۳ درخواست در ۱۰ دقیقه به‌ازای هر شماره.

**افزودن دستی کاربر** (درخواست بخش ۹): ادمین بتواند کاربر را بدون فرایند OTP بسازد و مستقیم `isVerified=True` کند — برای وقتی پیامک نمی‌رسد.

---

### content

**`BlogPost`** — `title`, `slug`, `excerpt`, `sections` (JSON: `{id, heading, body}[]`), `category`, `coverImage`, `author`, `readingTime`, `publishedAt`, `isPublished`, `metaTitle`, `metaDescription`

> این بخش در فهرست ۱۴‌تایی نبود ولی سایت بلاگ دارد و ۱۲ مقاله در آن است — بدون پنل مدیریت بلاگ، محتوایش قابل به‌روزرسانی نیست.

**`ContactMessage`** — `name`, `email`, `subject`, `message`, `isRead`, `adminNote`, `createdAt`, `ipAddress`

**`ProductReview`** (بخش ۱۴) — `product`, `user`, `rating` (۱ تا ۵), `title`, `body`, `status` (`pending` | `approved` | `rejected`), `adminReply`, `createdAt`
**فقط نظرات `approved` در سایت دیده شوند.** امتیاز میانگین و توزیع امتیاز محصول از همین‌ها محاسبه شود.
اختیاری ولی توصیه‌شده: فیلد `verifiedPurchase` که چک کند کاربر واقعاً آن محصول را خریده.

**`Coupon`** — `code`, `type` (`percent` | `fixed`), `value`, `minOrderValue`, `maxDiscount`, `usageLimit`, `usedCount`, `perUserLimit`, `startsAt`, `expiresAt`, `isActive`, `categories`/`products` (محدودسازی اختیاری)

> این هم در فهرست نبود، ولی هیچ فروشگاهی بدون کوپن راه نمی‌افتد و مدل سفارش از الان باید فیلد `discount` داشته باشد.

---

### settings

**`SiteSettings`** — تک‌رکوردی (singleton). شامل همه فیلدهای `SiteSettings` در قرارداد به‌علاوه:
`whatsappUrl`, `telegramUrl`, `instagramUrl`, `linkedinUrl`, `youtubeUrl`, `pinterestUrl`
`googleMapsEmbed` یا `latitude`/`longitude`
`trustBadgeImage`, `trustBadgeUrl` (نماد اعتماد الکترونیکی — تصویر و لینک واقعی، نه فقط برچسب)
`enamadCode` (کد اسکریپت نماد)
`logoLight`, `logoDark`, `favicon`
`defaultOgImage`
`googleAnalyticsId`, `googleTagManagerId`

**`ApiCredential`** — `service` (`kavenegar` | `zarinpal` | `idpay` | `snapppay` | `digipay` | ...), `label`, `credentials` (JSON **رمزنگاری‌شده**), `isActive`, `isSandbox`, `order`

**رفتار مورد نظر:** هر سرویس یک ردیف. `isActive=False` یعنی آن درگاه در صفحه چک‌اوت **اصلاً نمایش داده نشود**. کلیدها از پنل قابل تعویض باشند بدون دیپلوی مجدد.

**`ShippingMethod`** — `name`, `cost`, `freeAbove`, `estimatedDays`, `isActive`, `order`

---

## ۳. لایه پرداخت

یک کلاس انتزاعی `PaymentProvider` با متدهای `request(order)` و `verify(callbackData)`.
پیاده‌سازی جدا برای زرین‌پال، آیدی‌پی، اسنپ‌پی، دیجی‌پی. افزودن درگاه پنجم نباید هیچ کد مشترکی را عوض کند.

هر درگاه حالت sandbox داشته باشد. کلیدها از `ApiCredential` خوانده شوند نه از متغیر محیطی.

جریان: ایجاد `Payment` با `idempotencyKey` ← ریدایرکت ← کال‌بک ← `verify` سمت سرور ← اگر موفق، سفارش `paid` و `StockMovement` از نوع `sale` ثبت شود.

---

## ۴. بخش ۷ — پنل سرچ کنسول گوگل

نیازمند یک پروژه Google Cloud، فعال‌سازی **Google Search Console API**، احراز هویت OAuth 2.0 یا Service Account، و تأیید مالکیت دامنه `vybeshop.ir`.

داده‌هایی که API می‌دهد و ارزش نمایش دارند:

- **عملکرد جستجو:** ایمپرشن، کلیک، CTR، میانگین جایگاه — با بازه زمانی
- **عبارت‌های جستجو:** پرترافیک‌ترین کوئری‌هایی که سایت را آورده‌اند
- **صفحات:** پربازدیدترین صفحات از جستجو
- **وضعیت ایندکس:** چند صفحه ایندکس شده، چند تا خطا دارد، دلیل هرکدام
- **وضعیت sitemap:** آخرین خوانده‌شدن، تعداد URL کشف‌شده
- **مشکلات موبایل و Core Web Vitals**

توکن‌ها در `ApiCredential` ذخیره شوند. داده‌ها با Celery روزی یک‌بار کش شوند، نه در هر بار باز کردن صفحه — API سهمیه دارد.

---

## ۵. بخش ۱۱ — مدیریت فروش

گزارش‌ها با فیلتر بازه زمانی و خروجی اکسل:

فروش کل و تعداد سفارش به تفکیک روز/هفته/ماه · میانگین ارزش سفارش · پرفروش‌ترین محصولات به تعداد و به مبلغ · فروش به تفکیک دسته‌بندی · نرخ تبدیل (سبد ساخته‌شده در مقابل سفارش پرداخت‌شده) · سبدهای رهاشده · مشتریان بازگشتی در مقابل جدید · فروش به تفکیک درگاه پرداخت · نرخ مرجوعی · سود ناخالص در صورت ثبت قیمت تمام‌شده

پیشنهاد: فیلد `costPrice` به محصول اضافه شود تا حاشیه سود قابل محاسبه باشد.

---

## ۶. بخش ۱ — داشبورد

کارت‌های خلاصه: فروش امروز و این ماه · سفارش‌های در انتظار پردازش · سفارش‌های پرداخت‌نشده · کالاهای زیر نقطه سفارش · پیام‌های خوانده‌نشده · نظرات در انتظار تأیید · کاربران جدید این هفته
نمودار فروش ۳۰ روز اخیر · آخرین ۱۰ سفارش · پرفروش‌های هفته

---

## ۷. بخش ۳ — پنل اصلاح قیمت

- نمایش جدولی همه محصولات با قیمت فعلی و فیلد ویرایش درجا
- انتخاب چندتایی با چک‌باکس + فیلتر بر اساس دسته‌بندی
- اعمال گروهی: افزایش/کاهش **درصدی**، افزایش/کاهش **مبلغ ثابت**، یا ست کردن قیمت مشخص
- **پیش‌نمایش قبل از اعمال** — جدول قیمت فعلی در برابر قیمت جدید
- ذخیره یکجا در یک تراکنش
- گرد کردن اختیاری به نزدیک‌ترین هزار تومان
- **`PriceHistory`**: هر تغییر قیمت با کاربر، زمان، مقدار قبل و بعد ثبت شود. بدون این، اشتباه گروهی قابل برگشت نیست

---

## ۸. بخش‌هایی که در فهرست نبود و توصیه می‌کنم اضافه شوند

| بخش | چرا |
|---|---|
| **مدیریت بلاگ** | سایت بلاگ دارد، بدون پنل قابل به‌روزرسانی نیست |
| **کوپن و تخفیف** | مدل سفارش از الان به فیلد `discount` نیاز دارد |
| **روش‌های ارسال** | چک‌اوت بدون آن کامل نمی‌شود |
| **مرجوعی** | «وضعیت مرجوعی» فیلد محصول است، ولی خود فرایند مرجوعی مدل می‌خواهد |
| **لاگ فعالیت ادمین** | چه کسی چه چیزی را کِی تغییر داد. با چند کاربر ادمین حیاتی است |
| **اعلان‌های تراکنشی** | پیامک و ایمیل: تأیید سفارش، ارسال مرسوله، کد رهگیری |
| **پشتیبان‌گیری خودکار** | دیتابیس و فایل‌های آپلودی |

---

## ۹. ترتیب اجرا

| فاز | محتوا |
|---|---|
| **B1** | اسکلت پروژه، Docker، مدل‌های `catalog` + `content`، ادمین جنگو اولیه، اندپوینت‌های عمومی منطبق بر `API-CONTRACT.md` |
| **B2** | اتصال فرانت: بدنه توابع `src/lib/api.ts` از داده فیک به `fetch` واقعی برود. **هیچ کامپوننتی نباید تغییر کند** — اگر لازم شد، یعنی جایی قرارداد نقض شده |
| **B3** | `users` + OTP کاوه‌نگار + JWT + آدرس‌ها |
| **B4** | `orders`: سبد، چک‌اوت، ماشین حالت سفارش، `inventory` و کاردکس |
| **B5** | پرداخت: لایه انتزاعی + زرین‌پال، بعد بقیه درگاه‌ها |
| **B6** | `settings`، `analytics`، سرچ کنسول، گزارش‌های فروش |

بعد از هر فاز: مهاجرت، تست، مستندات OpenAPI، کامیت.

---

## ۱۰. پنل ادمین — قالب «هُما»

قالب در `admin-template/` است. **فقط خواندنی — مثل `design/` هرگز داخلش ننویس.**

### وضعیت قالب

HTML ایستا + Tailwind ۳ + وانیلا جاوااسکریپت + Chart.js. پنج صفحه دارد: `index` (داشبورد)، `orders`، `products`، `customers`، `analytics`. فونت وزیرمتن. تم تیره با کارت‌های شیشه‌ای.

سیستم طراحی‌اش در `admin-template/tailwind.config.js` و کلاس‌های کامپوننتی در `assets/css/tailwind.css` (`glass-card`، `nav-item`، `chip`، `stat-icon`، `notif-item`) تعریف شده.

### تصمیم معماری: بازنویسی به React، نه ادامه HTML ایستا

قالب را **مرجع طراحی** بدان، نه کد نهایی — دقیقاً همان‌طور که با `design/*.html` رفتار کردی.

دلیل: چهارده بخش با فرم‌های سنگین، ویرایش گروهی قیمت، مشخصات پویای EAV، جدول‌های فیلترشونده و آپلود چندتایی تصویر با وانیلا جاوااسکریپت غیرقابل نگهداری می‌شود. ضمناً استک React + TS + `react-hook-form` + `zod` + TanStack Query از قبل در پروژه هست.

**مکان:** یک اپ Vite جدا در `/admin` همان ریپو. مشترک با فرانت اصلی: تایپ‌ها، `cn()`، الگوی `api.ts`، ابزار فرمت. جدا: کامپوننت‌های بصری، چون زبان بصری پنل با فروشگاه فرق دارد.

### تم — قالب با برند بوک تضاد دارد، اصلاحش کن

| | قالب هُما | VYBE |
|---|---|---|
| پس‌زمینه | `#070b12` سرمه‌ای | `#0B0B0C` گرافیت |
| تأکید | `#10b981` سبز | `#00D1FF` سیان |
| فونت | وزیرمتن | **پیدا** (از قبل در `public/fonts/peyda/`) |
| سایه | `glow`, `card`, `lift` | برند بوک سایه را رد می‌کند، حاشیه به‌جایش |

**فقط `tailwind.config.js` را عوض کن، نه مارک‌آپ را.** چون قالب همه‌جا از کلاس‌های سمانتیک کانفیگ استفاده می‌کند، تغییر تم تقریباً کل کار است:

- پالت `ink-*` را به مشتقات گرافیت `#0B0B0C` ببر
- `brand-*` را از سبز به سیان `#00D1FF` ببر — ولی **فقط برای عناصر تعاملی**، نه سطح بزرگ
- فونت را به پیدا عوض کن (فایل‌های وزیرمتن حذف شوند، حجم کمتر)
- سایه‌های `glow` را نگه دار **فقط** در پنل ادمین. برند بوک درباره تجربه مشتری حرف می‌زند، نه ابزار داخلی — ولی شدتشان را کم کن

انیمیشن‌های `floaty`، `spin-slow` و `marquee-x` را حذف کن. یک پنل مدیریتی که روزی هشت ساعت جلوی چشم است نباید عنصر شناور داشته باشد.

### چهارده بخش در برابر پنج صفحه قالب

| بخش | صفحه قالب |
|---|---|
| ۱ داشبورد | ✅ `index.html` |
| ۲ محصولات | ✅ `products.html` (فقط لیست — فرم افزودن/ویرایش باید ساخته شود) |
| ۶ سفارشات | ✅ `orders.html` (لیست — صفحه جزئیات سفارش باید ساخته شود) |
| ۹ کاربران | ✅ `customers.html` |
| ۱۱ مدیریت فروش | ✅ `analytics.html` |
| ۳ اصلاح قیمت · ۴ دسته‌بندی · ۵ مشخصات محصولات · ۷ سرچ کنسول · ۸ موجودی · ۱۰ پیام‌ها · ۱۲ تنظیمات · ۱۳ کاردکس · ۱۴ نظرات | ❌ باید ساخته شود |

برای صفحات جدید **الگوهای همان قالب را تکرار کن** — همان `glass-card`، همان ساختار جدول، همان سایدبار. صفحه‌ای که شبیه بقیه نباشد اشتباه است.

### الزامات هر صفحه پنل

- هر جدول: صفحه‌بندی سمت سرور، مرتب‌سازی، فیلتر در query string
- هر لیست: چهار حالت پر / اسکلتون / خالی / خطا
- هر فرم: `react-hook-form` + `zod`، پیام خطای فارسی، حالت در حال ذخیره، تأیید قبل از خروج در صورت تغییر ذخیره‌نشده
- هر عملیات مخرب: دیالوگ تأیید
- RTL کامل، همان قواعد پراپرتی منطقی فرانت اصلی
- دسترسی نقش‌محور: هر بخش بررسی مجوز داشته باشد، نه فقط مخفی‌کردن آیتم منو

---

## ۱۱. قرارداد API پنل

قبل از نوشتن هر صفحه‌ای از پنل، فایل `ADMIN-API-CONTRACT.md` را بساز — با همان دقت و فرمت `API-CONTRACT.md` موجود.

برای هر بخش: اندپوینت‌ها، پارامترها، شکل درخواست و پاسخ، مجوز لازم، و خطاهای ممکن.
پنل روی این قرارداد ساخته می‌شود، پس اگر درست باشد، وصل‌کردن به بک‌اند واقعی فقط تعویض بدنه توابع است.

مسیرها زیر `/api/admin/` و جدا از API عمومی. احراز هویت JWT با نقش staff.

---

## ۱۲. ترتیب اجرا — بازنگری‌شده

| فاز | محتوا | وابستگی |
|---|---|---|
| **A0** | مدل‌های جنگو (همه اپ‌ها) + مهاجرت + ادمین جنگو موقت + `ADMIN-API-CONTRACT.md` | — |
| **A1** | اسکلت اپ `/admin`: Vite + React + TS، تم اصلاح‌شده از `tailwind.config.js` قالب، لایوت (سایدبار + هدر)، روتینگ، لایه `api.ts` با داده فیک | A0 |
| **A2** | صفحات پنل بر اساس قالب: داشبورد، محصولات (لیست + فرم)، سفارشات (لیست + جزئیات)، کاربران، تحلیل‌ها | A1 |
| **A3** | نُه صفحه باقی‌مانده با همان الگو | A2 |
| **B1** | اندپوینت‌های عمومی منطبق بر `API-CONTRACT.md` | A0 |
| **B2** | **اتصال فرانت فروشگاه:** بدنه توابع `src/lib/api.ts` از فیک به `fetch` واقعی. هیچ کامپوننتی نباید تغییر کند | B1 |
| **B3** | `users` + OTP کاوه‌نگار + JWT + آدرس‌ها | B1 |
| **B4** | `orders`: سبد، چک‌اوت، ماشین حالت، `inventory` و کاردکس | B3 |
| **B5** | پرداخت: لایه انتزاعی + زرین‌پال، بعد بقیه درگاه‌ها | B4 |
| **B6** | اندپوینت‌های `/api/admin/` و اتصال واقعی پنل | A3 + B4 |
| **B7** | تنظیمات، سرچ کنسول، گزارش‌های فروش | B6 |

**A0 گلوگاه همه چیز است — اول آن.** بعد از A0، مسیر پنل (A1→A3) و مسیر API عمومی (B1→B5) موازی‌اند.

بعد از هر فاز: مهاجرت، تست، مستندات OpenAPI، کامیت.

---

## ۱۳. تست

هر فاز حداقل: تست مدل برای منطق حیاتی (محاسبه سفارش، ماشین حالت، مانده کاردکس)، تست API برای همه اندپوینت‌های عمومی، و یک تست یکپارچه برای مسیر سبد تا پرداخت موفق.
تست `verify` تکراری پرداخت — نباید دوبار موجودی کم کند.
تست دسترسی: کاربر غیر staff نباید به هیچ مسیر `/api/admin/` دسترسی داشته باشد.
