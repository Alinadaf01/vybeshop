DEBUG=True
SECRET_KEY=change-me-in-production
ALLOWED_HOSTS=localhost,127.0.0.1

DATABASE_URL=postgres://vybeshop:vybeshop@localhost:5433/vybeshop

REDIS_URL=redis://localhost:6379/0

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174

# Fernet key for ApiCredential.credentials — generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FIELD_ENCRYPTION_KEY=

JWT_SIGNING_KEY=change-me-in-production

# Kavenegar (OTP SMS) — used by phase B3, key itself should move to ApiCredential
# once the settings app exists; kept here only as a local-dev fallback.
KAVENEGAR_API_KEY=
