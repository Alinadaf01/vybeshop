# تسک جامع پایانی — قبل از دیپلوی بک‌اند

> **این تسک را پشت سر هم و بدون توقف انجام بده.** اگر جایی سؤالی داشتی که جواب قطعی ندارد، **بهترین تصمیم را خودت بگیر**، در `NOTES-FINAL.md` ثبت کن و ادامه بده — متوقف نشو.
>
> **دیپلوی بک‌اند در این تسک نیست.** بعد از تمام شدن این کارها متوقف شو و گزارش بده.

بعد از هر بخش: تست، `npm run check`، تست بک‌اند، و کامیت جدا.

---

## بخش ۱ — آیکون‌های هدر

الان هدر از برچسب متنی مونو استفاده می‌کند: `SEARCH` · `CART` · `ACCOUNT` · `MENU`. این با حس صنعتی برند می‌خواند ولی برای مشتری عادی فوری قابل تشخیص نیست.

**راه‌حل: آیکون + حفظ برچسب مونو در دسکتاپ، فقط آیکون در موبایل.** هم شخصیت برند حفظ می‌شود هم تشخیص فوری.

- `lucide-react` نصب شود — آیکون‌های خطی هندسی که با گوشه‌های گرد لوگو می‌خوانند
- آیکون‌ها: `Search` · `ShoppingBag` · `User` · `Menu` (و `X` برای بستن)
- `strokeWidth={1.5}`، اندازه ۲۰px، رنگ از `currentColor` تا با هاور عوض شود
- **دسکتاپ:** آیکون + برچسب مونو کنار هم با فاصله ۸px (طبق برند بوک: «فاصله آیکون تا متن ۸ پیکسل، آیکون هم‌تراز ارتفاع متن»)
- **موبایل:** فقط آیکون، با `aria-label` فارسی روی هر دکمه
- ناحیه لمسی حداقل ۴۴×۴۴ پیکسل
- بج شمارنده سبد سر جای خودش بماند
- منوی موبایل باز/بسته بین `Menu` و `X` جابه‌جا شود

**همین آیکون‌ها در `MobileMenu` هم استفاده شوند تا یکدست باشد.**

---

## بخش ۲ — وسط‌چین کردن بلوک برند فوتر

در `Footer.tsx` بلوک بالایی شامل لوگو و شعار «طراحی‌شده برای ارتقای روزمرگی.» است.

**هر دو با هم وسط‌چین شوند**، نه فقط شعار — وگرنه شعار از لوگوی بالای سرش جدا می‌افتد و شبیه اشتباه چیدمانی می‌شود.

ستون‌های زیرش راست‌چین بمانند. بلوک برند وسط‌چین بالای ستون‌های راست‌چین، سلسله‌مراتب تمیزی می‌سازد.

---

## بخش ۳ — آیکون حالت خطای سبد خرید

در `CartPage` وقتی دریافت سبد ناموفق است، بالای پیام یک جای خالی آیکون هست.

- آیکون مناسب: `AlertCircle` یا `RefreshCw` از lucide
- اندازه ۴۸px، رنگ `danger-ink` (طبق قاعده کنتراست روی سطح روشن)
- **همه حالت‌های خالی و خطای سایت را بررسی کن** — این تنها جا نیست. سبد خالی، نتیجه‌ای یافت نشد، بدون سفارش، بدون آدرس، بدون علاقه‌مندی. هر کدام آیکون خطی متناسب بگیرد
- در پنل ادمین هم همین کار انجام شود

---

## بخش ۴ — دسترسی ادمین به فاکتور مشتری

اندپوینت `admin-order-invoice-pdf` وجود دارد ولی **در رابط پنل دکمه‌ای برایش نیست**.

- دکمه «دانلود فاکتور» در صفحه جزئیات سفارش پنل
- دکمه «فاکتور» در منوی هر ردیف لیست سفارشات
- **دقیقاً همان PDF که مشتری می‌گیرد** — نه نسخه متفاوت
- کنارش دکمه «برگه بسته‌بندی» هم باشد، چون هر دو همراه بسته پرینت می‌شوند
- فقط برای سفارش‌های پرداخت‌شده به بعد
- در حالت بارگذاری دکمه غیرفعال شود، خطا با توست نمایش داده شود

---

## بخش ۵ — بازطراحی کامل اسناد PDF ⭐ مهم‌ترین بخش این تسک

قالب‌های فعلی کار می‌کنند ولی طراحی ندارند. کارفرما روی این حساس است و این اسناد **رو در روی مشتری** قرار می‌گیرند.

### قواعد مشترک همه اسناد

**رنگ — فقط از پالت برند:**
```
گرافیت   #0B0B0C   متن اصلی، سربرگ
سفید مه‌آلود #F5F5F3   پس‌زمینه ردیف‌های متناوب
تیتانیوم  #7A7D82   برچسب‌ها، متن ثانویه
خاکستری روشن #ECECEC  خطوط جدول
سیان     #00D1FF   فقط یک نوار باریک تأکیدی — حداکثر یک بار در کل سند
```

**بدون رنگ دیگر. بدون سایه. بدون گرادیان.** برند بوک: «به‌جای سایه از حاشیه استفاده شود.»

**تایپوگرافی:**
- متن فارسی: **پیدا** — عنوان ۶۰۰، متن ۴۰۰
- اعداد، کد کالا، ابعاد، مبالغ: **JetBrains Mono** با `dir="ltr"`
- اندازه‌ها: عنوان سند ۲۴pt · عنوان بخش ۱۴pt · متن ۱۰pt · جدول ۹pt · پاورقی ۸pt
- **فونت‌ها جاسازی شوند** تا PDF آفلاین هم درست باز شود

**ساختار صفحه A4:**
- حاشیه ۱۸mm از هر طرف
- **سربرگ:** لوگوی تخت VYBE سمت راست (ارتفاع ۱۲mm) · نام سند وسط · شماره و تاریخ سمت چپ
- یک خط افقی ۱px خاکستری زیر سربرگ
- **پاورقی روی هر صفحه:** `vybeshop.ir` سمت راست · شماره صفحه از کل وسط · تاریخ تولید سند سمت چپ
- سربرگ جدول در صفحات بعدی تکرار شود

**جدول‌ها:**
- ردیف سربرگ: پس‌زمینه گرافیت، متن سفید، وزن ۶۰۰
- ردیف‌های زوج پس‌زمینه `#F5F5F3`
- خط جداکننده ۱px `#ECECEC` — بدون کادر دور کل جدول
- ستون‌های عددی راست‌چین با فونت مونو
- ارتفاع ردیف ۸mm، پدینگ افقی ۳mm

### فاکتور فروش — دقیق‌ترین سند

ترتیب بلوک‌ها از بالا:

۱. **سربرگ** طبق بالا، با عنوان «فاکتور فروش»
۲. **دو ستون روبه‌روی هم:** «فروشنده» (نام کسب‌وکار، آدرس، تلفن، کد اقتصادی، شناسه ملی — از تنظیمات سایت) و «خریدار» (نام، تلفن، آدرس تحویل)
۳. **نوار اطلاعات سفارش:** شماره فاکتور · شماره سفارش · تاریخ (شمسی) · روش پرداخت و نام درگاه · کد رهگیری تراکنش
۴. **جدول اقلام:** ردیف · شرح کالا · کد کالا (مونو) · تعداد · قیمت واحد · جمع
۵. **بلوک جمع‌بندی، راست‌چین در نیمه چپ صفحه:** جمع کل · تخفیف · هزینه ارسال · مالیات · **مبلغ نهایی** با وزن ۶۰۰ و اندازه ۱۴pt و یک خط بالایش
۶. **مبلغ به حروف** — ماژول `persian.py` این را دارد
۷. **کد رهگیری پستی** اگر ارسال شده
۸. **پانوشت:** «طراحی‌شده برای ارتقای روزمرگی.» ریز و کم‌رنگ

بدون کادر تزئینی، بدون واترمارک، بدون شعار تبلیغاتی.

### هشت سند دیگر

همان قواعد مشترک. تفاوت‌های خاص:

- **برگه بسته‌بندی:** قیمت **نمایش داده نشود** (داخل جعبه می‌رود، ممکن است هدیه باشد). فونت درشت‌تر برای خواندن سریع هنگام بسته‌بندی. کادر خالی برای تیک زدن کنار هر قلم
- **لیست ارسال روزانه:** فشرده، هر سفارش یک ردیف، ستون کد رهگیری خالی برای نوشتن دستی
- **صورت موجودی:** ستون «شمارش دستی» خالی برای انبارگردانی
- **گزارش فروش:** یک نمودار ساده بالای جدول اگر ممکن است، وگرنه فقط جدول
- **لیست قیمت:** گروه‌بندی بر اساس دسته، برای ارسال به مشتری عمده مناسب باشد

### تأیید

**هر نُه سند را واقعاً تولید کن و ببین.** نه فقط تست اینکه خطا نمی‌دهد — خروجی PDF را باز کن و بررسی کن که فارسی درست شکل گرفته، اعداد چپ‌چین نشده‌اند، صفحه‌بندی درست است و چیزی از لبه بیرون نزده.

---

## بخش ۶ — بازطراحی خروجی‌های اکسل

الان `sheet.append([...])` خام با سربرگ انگلیسی و بدون هیچ قالب‌بندی.

- **سربرگ فارسی** روی همه ستون‌ها
- جهت شیت **راست‌به‌چپ** (`sheet.sheet_view.rightToLeft = True`)
- ردیف سربرگ: پس‌زمینه گرافیت، متن سفید، بولد، ارتفاع ۲۴
- **فریز کردن ردیف سربرگ** تا هنگام اسکرول بماند
- عرض ستون‌ها متناسب با محتوا، نه پیش‌فرض
- **قالب عدد** روی مبالغ: `#,##0` با پسوند تومان
- تاریخ‌ها شمسی
- **فیلتر خودکار** روی ردیف سربرگ
- ردیف جمع در پایان برای گزارش‌های عددی
- نام شیت فارسی و معنادار
- یک ردیف بالای جدول: نام گزارش، بازه زمانی، فیلترهای اعمال‌شده، تاریخ تولید

هر خروجی اکسل باید **همان فیلترهای فعال جدول** را اعمال کند، نه کل داده.

---

## بخش ۷ — رفع بدهی‌های فنی باقی‌مانده

### ۷.۱ پری‌رندر هوم و بلاگ

در F6 عمداً کنار گذاشته شد. حالا با `AppProviders` و ساختار فعلی قابل انجام است: prefetch کوئری‌ها در `entry-server`، سپس dehydrate کش داخل HTML تا هنگام hydrate دوباره fetch نشود و صفحه نپرد.

### ۷.۲ داده محصولات با برند نمی‌خواند

از ۲۴ محصول، ۸ تا «آلومینیوم CNC شده» و ۱۱ تا «پلی‌کربنات» هستند و هیچ‌کدام PLA نیست — در حالی که VYBE کارگاه چاپ سه‌بعدی است و صفحه درباره ما همین را می‌گوید. **سایت با خودش تناقض دارد.**

متریال‌ها فقط `PLA+ مات`، `PETG`، `TPU` باشند. توضیحات از «بلوک آلومینیوم CNC‌شده» و «لولای فلزی» پاک شوند. قطعات جانبی واقعی (مگنت، پیچ، پد سیلیکونی) بمانند ولی به‌عنوان **جانبی** معرفی شوند نه جنس بدنه.

هم `src/data/products.ts` هم دستور `seed_storefront` به‌روز شوند.

### ۷.۳ پاکسازی قبل از انتشار

- همه کاربران staff تستی حذف شوند
- superuser توسعه `09120000000` اگر هست حذف شود
- محصولات دیباگ (`DBG-*`) از داده seed حذف شوند
- `.env.example` بررسی شود که هیچ کلید واقعی داخلش نباشد

### ۷.۴ اندازه‌گیری دوباره پرفورمنس

اعداد قبلی بدون هیچ تصویری گرفته شده بودند. با وضعیت فعلی دوباره اندازه بگیر: LCP، CLS، حجم جاوااسکریپت اولیه. اگر از بودجه (LCP < 2.5s، CLS < 0.1، JS < 200KB gzip) بیرون زد، گزارش بده.

### ۷.۵ بازبینی امنیتی

- rate limit روی OTP، ورود، فرم تماس، و ثبت سفارش
- تنظیمات production جنگو: `DEBUG=False`، `ALLOWED_HOSTS`، `SECURE_*`
- بررسی که هیچ اندپوینت ادمینی بدون مجوز باز نمانده
- بررسی که کلیدهای API در هیچ پاسخ یا لاگی ظاهر نمی‌شوند

---

## بخش ۸ — آماده‌سازی برای دیپلوی (بدون اجرای دیپلوی)

فقط **آماده کن**، اجرا نکن:

- `Dockerfile` برای بک‌اند production
- `docker-compose.prod.yml` با Postgres، Redis، Celery worker، Celery beat، Nginx
- تنظیمات جدا برای production
- سرو فایل‌های استاتیک و مدیا
- `.env.production.example` با همه متغیرهای لازم و توضیح هرکدام
- یک `DEPLOY.md` که مرحله‌به‌مرحله بنویسد چه باید کرد

**دیپلوی واقعی را انجام نده.** فقط همه چیز را آماده بگذار.

---

## در پایان

یک `NOTES-FINAL.md` بنویس شامل: چه چیزی تمام شد، چه تصمیم‌هایی خودت گرفتی، چه چیزی باقی ماند، و هر سؤالی که برای دیپلوی از کارفرما لازم داری (دامنه، سرور، دسترسی‌ها).
